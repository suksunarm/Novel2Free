<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      ::-webkit-scrollbar {
        width: 8px;
      }
      ::-webkit-scrollbar-track {
        background: rgba(0, 0, 0, 0.1);
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb {
        background: rgba(0, 0, 0, 0.3);
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: rgba(0, 0, 0, 0.5);
      }
    </style>
  </head>
  <body class="font-sans">
    <div
      class="bg-[url('/assets/bg.png')] bg-cover bg-center min-h-screen w-full items-center"
    >
      <div class="bg-black/60 backdrop-blur-sm border-b border-white/10">
        <div class="max-w-6xl mx-auto px-4 py-4">
          <h1
            class="text-2xl md:text-3xl font-bold text-white flex items-center gap-3"
          >
            <span class="text-orange-400">🛒</span>
            Your Novel Cart
          </h1>
        </div>
      </div>

      <div class="max-w-lg mx-auto px-4 py-8">
        <div
          class="bg-white/95 backdrop-blur-sm rounded-2xl shadow-xl overflow-hidden"
        >
          <% if(cartItems.length === 0) { %>
          <!-- Empty Cart UI -->
          <div class="p-10 text-center">
            <span class="text-6xl mb-4 block">🛒</span>
            <h2 class="text-2xl font-bold text-gray-800 mb-2">
              Your cart is empty
            </h2>
            <p class="text-gray-600 mb-6">
              Looks like you haven't added any novel yet.
            </p>
            <a
              href="/"
              class="inline-block bg-orange-500 hover:bg-orange-600 text-white font-bold py-3 px-6 rounded-xl transition-colors shadow-lg hover:shadow-xl"
            >
              Browse Novel
            </a>
          </div>
          <% } else { %>
          <!-- Cart Items Loop -->
          <div class="divide-y divide-gray-100">
            <% cartItems.forEach(item => { %>
            <div class="p-6 hover:bg-gray-50/80 transition-colors">
              <div class="flex flex-col sm:flex-row gap-4">
                <div class="flex-shrink-0">
                  <img
                    src="<%= item.novel_id.image_url || 'https://via.placeholder.com/120x160' %>"
                    alt="Novel Cover"
                    class="w-24 h-32 sm:w-30 sm:h-40 rounded-lg shadow-md object-cover mx-auto sm:mx-0"
                  />
                </div>

                <div class="flex-grow min-w-0">
                  <div
                    class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-4"
                  >
                    <div class="min-w-0 flex-grow">
                      <h2
                        class="text-lg sm:text-xl font-bold text-gray-900 line-clamp-2 break-words"
                      >
                        <%= item.novel_id.title %>
                      </h2>
                      <div class="flex items-center gap-2 mt-2">
                        <span
                          class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-orange-100 text-orange-800"
                        >
                          Novel
                        </span>
                        <% if(item.novel_id.volume) { %>
                        <span class="text-sm text-gray-500"
                          >Volume <%= item.novel_id.volume %></span
                        >
                        <% } %>
                      </div>
                      <% if(item.novel_id.description) { %>
                      <p class="text-sm text-gray-600 mt-2 line-clamp-2">
                        <%= item.novel_id.description %>
                      </p>
                      <% } %>
                    </div>

                    <div
                      class="flex flex-row sm:flex-col items-center sm:items-end gap-3 sm:gap-2 flex-shrink-0"
                    >
                      <div class="text-right">
                        <div
                          class="text-xl sm:text-2xl font-bold text-orange-600"
                        >
                          ฿<%= item.price %>
                        </div>
                      </div>
                      <form method="POST" action="/cart/remove/<%= item._id %>">
                        <button
                          class="px-4 py-2 text-sm font-medium text-red-700 bg-red-50 border border-red-200 rounded-lg hover:bg-red-100 hover:border-red-300 transition-colors"
                        >
                          Remove
                        </button>
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <% }) %>
          </div>

          <!-- Summary -->
          <div
            class="bg-gray-50/80 backdrop-blur-sm p-6 border-t border-gray-200"
          >
            <div class="max-w-md ml-auto">
              <div class="space-y-3">
                <div class="flex justify-between items-center text-gray-700">
                  <span class="font-medium">Total Items:</span>
                  <span class="font-bold text-lg"><%= totalItems %></span>
                </div>
                <div class="flex justify-between items-center text-gray-700">
                  <span class="font-medium">Subtotal:</span>
                  <span class="font-bold text-lg">฿<%= totalPrice %></span>
                </div>
                <div class="border-t border-gray-300 pt-3">
                  <div class="flex justify-between items-center">
                    <span class="text-lg font-bold text-gray-900">Total:</span>
                    <span class="text-2xl font-bold text-orange-600"
                      >฿<%= totalPrice %></span
                    >
                  </div>
                </div>
              </div>

              <div class="mt-6 space-y-3">
                <button
                  class="w-full bg-orange-500 hover:bg-orange-600 text-white font-bold py-4 px-6 rounded-xl text-lg transition-colors shadow-lg hover:shadow-xl transform hover:scale-105 duration-200"
                >
                  🚀 Proceed to Checkout
                </button>
                <a
                  href="/"
                  class="block w-full text-center bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium py-3 px-6 rounded-xl transition-colors"
                >
                  Continue Shopping
                </a>
              </div>
            </div>
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/script.js"></script>
  </body>
</html>
